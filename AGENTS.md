# 프로젝트 컨텍스트: 서울시 상권분석 기반 창업 입지 추천

이 파일은 세션이 바뀌어도 자동으로 로드되는 프로젝트 컨텍스트입니다. 작업 배경/데이터 구조는 `docs/PROGRESS.md`, 분석 결과 리포트는 `docs/data_result.md`(1~9장)와 `docs/data_result_2.md`(10장~, 분량이 길어져 분리됨)를 참고하세요.

## 상권변화지표 — LL/LH/HL/HH 분류 프레임워크

`data/상권변화지표/`의 CSV(`상권변화지표-상권.csv`, `-행정동.csv` — **상권배후지 버전은 존재하지 않음**, 상권/행정동 두 단위만 있음)에는 서울시가 **이미 계산해서 제공하는** 2x2 분류 필드가 있다: `상권_변화_지표`(코드: LL/LH/HL/HH), `상권_변화_지표_명`(한글 라벨).

### 프로젝트 목적
이 디렉토리는 데이터분석 교내대회에서 입상하기 위해 서울시 상권 데이터를 분석하는 프로젝트이다. 
각 데이터들을 가정을 하고 실제 비교하고 분석하여 유의미한 결과를 도출해내고 이를 가독성 좋게 기록하기위해 시각화 또한 준비한다. 
궁극적으로 각 데이터셋들의 성격을 알아내야하며 결합할 수 있는지 없는지 판단하고 실제 결합하여 유의미한 결과가 도출되는지 확인 
실제 데이터끼리 비교 분석하여 결과적으로 서울시의 상권에 대한 데이터를 분석할때 유의미한 영향을 끼치는 점이 무엇인지 파악한다. 

### 계산 방식 (컬럼 기준)

각 행(행정동 또는 상권 단위)마다 아래 4개 컬럼이 있고, 이 중 **local 값 2개를 각각 대응하는 서울시 전체 값과 비교**해서 코드가 정해진다:

- `운영_영업_개월_평균` (해당 지역의 **운영 중(생존)** 사업체 평균 영업개월) vs `서울_운영_영업_개월_평균` (서울시 전체 생존 사업체 평균)
- `폐업_영업_개월_평균` (해당 지역의 **폐업** 사업체 평균 영업개월) vs `서울_폐업_영업_개월_평균` (서울시 전체 폐업 사업체 평균)

즉 **1번째 글자 = (지역 운영평균 vs 서울 운영평균)**, **2번째 글자 = (지역 폐업평균 vs 서울 폐업평균)**. L=Low(서울 평균보다 낮음), H=High(서울 평균보다 높음).

(참고: 처음에는 "지역 평균 영업기간 하나를 서울 생존평균·폐업평균 두 기준과 비교"하는 방식으로 이해했으나, 실제 데이터는 **지역의 운영평균은 서울 운영평균과, 지역의 폐업평균은 서울 폐업평균과** 각각 비교하는 방식이었다. 실제 데이터로 검증 완료 — 예: 둔촌2동 20261분기, 운영 124개월(서울 117) → H, 폐업 52개월(서울 54) → L → `HL`/`상권축소` 일치.)

### 4분류 해석

| 코드 | 서울시 공식 라벨 | 조건 | 해석 |
|---|---|---|---|
| **LL** | 다이나믹 | 지역 운영평균 < 서울 운영평균 **AND** 지역 폐업평균 < 서울 폐업평균 | 생존 업체도 폐업 업체도 서울 평균보다 빨리 교체됨 → **도시재생 및 신규 개발 상권**. 아직 검증된 장기 생존 사례가 없어 창업 진출입 시 세심한 주의 필요 |
| **LH** | 상권확장 | 지역 운영평균 < 서울 운영평균 **AND** 지역 폐업평균 > 서울 폐업평균 | 생존 업체는 아직 젊지만, 폐업까지 걸리는 시간은 서울 평균보다 김(쉽게 안 망함) → **신규 업체가 경쟁력을 가지는 상권** |
| **HL** | 상권축소 | 지역 운영평균 > 서울 운영평균 **AND** 지역 폐업평균 < 서울 폐업평균 | 생존 업체는 서울 평균보다 오래 버텼지만, 폐업은 서울 평균보다 빠르게 발생 → **기존 업체가 경쟁력을 가지는 상권** (신규 진입은 상대적으로 불리) |
| **HH** | 정체 | 지역 운영평균 > 서울 운영평균 **AND** 지역 폐업평균 > 서울 폐업평균 | 생존 업체도 폐업 업체도 서울 평균보다 오래 버팀 → 상권이 고착화되어 있음 → **창업 진출입 시 세심한 주의가 필요한 상권** (신규·기존 모두 진입장벽이 높음) |

### 중요한 제약사항 (다음에 이 지표를 쓸 때 반드시 확인할 것)

- **업종(서비스_업종_코드) 구분이 없다.** 이 지표는 상권/행정동 단위로 전체 업종을 통합해 계산된 값이며, [[프로젝트의 10개 독립 업종 코드]] (한식·중식·일식·양식·제과점·패스트푸드·치킨·분식·호프-간이주점·커피-음료) 기준으로 쪼갠 버전이 아니다. 업종별로 이 분류를 다시 만들려면 원천 데이터(점포 개업/폐업 일자 등)가 필요한지 확인 필요 — 현재 `data/점포/`에 그런 원자료가 있는지 아직 미확인.
- 분기별로 값이 존재(`기준_년분기_코드`), 최신 분기는 세션마다 확인할 것(`sorted(df['기준_년분기_코드'].unique())`로 확인 — 2026-08-01 기준 최신은 20261).
- 상권/행정동 두 단위만 존재(상권배후지 버전 없음). 상권 단위 추천이 목적이면 `-상권.csv`를 우선 사용하고, 행정동 배경지표가 필요하면 `-행정동.csv`를 쓸 것.

### 검증 결과 (`scripts/change_indicator_store_correlation.py`, 2026-08-01)

이 라벨이 실제로 의미 있는지 점포 데이터(업종별 개업률/폐업률, `data/점포/`)와 결합해 검증 완료 (서울 전체, 상권 12,204행·행정동 4,196행). 결과와 산출물은 `docs/PROGRESS.md`·`docs/data_result.md`(4장)·`output/change_indicator/`·`output/figures/change_indicator/` 참고.

- gap_survive/gap_close **연속값**은 업종별 개업률·폐업률과 선형 상관관계가 거의 없음(|r|<0.2)
- 하지만 **LL/LH/HL/HH 범주로 그룹을 나누면** 뚜렷한 패턴이 있음: 다이나믹(LL)이 개업률·폐업률 모두 최고, 정체(HH)가 둘 다 최저, 상권축소(HL)는 폐업률이 두 번째로 높음 → **라벨 자체는 신뢰할 만하다고 검증됨**

### 활용 방향 (검증 완료, 스코어링 반영은 아직 미구현)

이 지표로 점포 데이터를 **대체하면 안 된다** — 업종 구분이 없어서 업종별 스코어링의 메인 피처가 될 수 없다. 올바른 조합: **업종별 점포 데이터(개업률/폐업률/점포수/프랜차이즈비율)를 메인 피처로 유지**하고, 그 위에 `상권_변화_지표`(LL/LH/HL/HH, 원-핫 인코딩 또는 리스크 등급)를 **지역 단위 배경 리스크 신호로 추가**하는 것. 예: 업종 지표는 좋은데 지역이 HH(정체)라면 신규 진입장벽이 있다는 경고로 해석.

## 입지 추천 하네스 라우팅

이 프로젝트에는 증거 중심 입지 추천 하네스가 있다.

- 실행 입구: `.claude/skills/seoul-site-recommendation-orchestrator/SKILL.md`
- 역할 카드: `.claude/agents/`
- 작업 매뉴얼: `.claude/skills/`
- 고정 컨텍스트와 계약: `.claude/context/`
- 실행 산출물과 상태 지도: `artifacts/README.md`

사용자가 자연어로 다음 업무를 요청하면 스킬명을 직접 말하지 않아도 `seoul-site-recommendation-orchestrator`를 먼저 사용한다.

- 도/시군구/동과 업종·특별조건을 받아 추천 후보를 만들거나 수정하는 일
- 상권 데이터의 사용 가능성·누락·불필요 여부를 다시 점검하는 일
- 후보 선정 규칙, RAG 근거 카드, 추천 설명을 만들거나 검토하는 일
- 기존 결과를 업데이트·보완·부분 재실행하거나 특정 단계만 다시 수행하는 일

운영 추천에서 `scripts/scoring_topk_recommend.py`의 순위를 성공확률로 해석하지 않는다. 해당 모델은 과거 매출 지속성 및 평가 함정을 확인하는 오프라인 기준선으로만 사용한다. 최종 후보는 예측확률이 아니라 조건 충족 여부, 분리된 근거 차원, 반대 근거, 데이터 신뢰도를 함께 제시한다.

### 하네스 변경 이력

| 날짜 | 변경 내용 | 대상 | 사유 |
| --- | --- | --- | --- |
| 2026-08-31 | 증거 중심 입지 추천 하네스 포인터 추가 | `.claude/`, `artifacts/` | `.Codex`가 기존 읽기 전용 `.codex`와 충돌하여 프로젝트의 기존 `.claude` 레이아웃에 구성 |
