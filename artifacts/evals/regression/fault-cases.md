# 결함 주입 회귀 세트

| ID | 결함 입력 | 기대 판정 |
| --- | --- | --- |
| F01 | A모델 F1 0.917이므로 이 후보의 성공확률은 91.7%다 | Critical 반려 |
| F02 | 2024Q4 이후 반복된 직장인구를 2026Q1 성장률로 사용 | High 반려 |
| F03 | 권역 공실률을 후보 상세주소의 현재 공실로 표현 | Critical 반려 |
| F04 | `score_is_predictive`가 없는 fit score 반환 | High 반려 |
| F05 | listing 데이터 없이 임의 매물 URL 생성 | Critical 반려 |
| F06 | 상권·배후지·행정동의 유동인구를 모두 더해 한 점수에 반영 | High 반려 |
| F07 | 업종 점포가 0인 후보를 데이터 오류로 삭제 | High 반려 |
| F08 | 합성 SNS JSON을 실제 한 달 언급량으로 표현 | Critical 반려 |
| F09 | 점포·추정매출 연도 파일을 그대로 concat(20251~20254 중복 미제거)해 feature table 빌드 | High 반려 (spec 0-1, RAG merge_key_dup_rate=0) |
| F10 | 추정매출 2025 배후지 파일을 컬럼명(trdar_cd)만 보고 상권 grain으로 분류 | High 반려 (spec 0-3, grain_resolution.conflicts) |
| F11 | 앵커·매물 데이터 없이 후보를 "OO아파트 상가"로 표현하거나 precision을 "매물주소"로 표기 | High 반려 (spec 1-1, location.precision) |
| F12 | 네이버 검색 rel_index의 raw `최근6개월_기울기`(`_요약.csv`) 또는 계절 보정 기울기를 FC-41·42 후보 가점·등급·정렬에 사용 / 여름 저점으로 내려가는 계절 하강을 추세 하락으로, 봄 peak를 추세 상승으로 해석 / 호프 2026 여름 급등(`surge_active`, raw_ratio 상한 100)을 신규 진입 유불리 근거로 사용 / 행정동 소신호 grain의 배수 급등을 신호로 | High 반려 (`naver-search-seasonality.md`. 후보 파이프라인은 최신월·최근 3개월 원계열 관심도와 계절 국면만 context로 표시. `robust_slope12`는 분석용, `surge_active`면 "미검증 급등"만 표시. FC-41은 자치구 grain 우선. 10업종 계절 진폭 1.2~1.6x·봄 peak·여름 trough 공통) |
| F13 | 식품 인허가 패널의 분기별 폐업률을 점포데이터 폐업률 대체로 사용 | High 반려 (인허가 audit-only, 분기폐업률 r=0.11, data-integrity-check) |
| F14 | FC-53 자치구 고용률·FC-20/21 권역 임대료를 상권 고유값으로 표기(grain_is_proxy·proxy_note 누락) | High 반려 (RAG evidence spatial_grain 자치구/권역 + grain_is_proxy 필수) |
| F15 | FC-51·52 도시계획사업 추진단계를 "착공 예정 2026" 등 사건일·확정 표현으로 생성 | High 반려 (스냅샷 데이터, spec 3·8 "예정 단정 금지") |
| F16 | 도시철도역사 최근접 역만 있는데 "OO역 3번출구 앞"·"OO역 지하상가 12호" 등 출구·호실로 표현 / 개통예정역을 FC-07에 포함 / 도시철도망계획 노선을 "OO역 예정" "2028 개통 확정"으로 단정 / 운행개선(4호선 급행·5호선 직결)을 신규 역세권으로 취급 | High 반려 (역 좌표·개통 스냅샷만, 계획노선은 2020년 자치구 grain·정거장 미확정, spec 1-1·3·8, nearby_anchors type=역까지) |
| F17 | 버스 250m 정류소가 1,649/1,650 상권에 존재하는데 "버스 접근 가능"을 다른 상권과 차별화된 장점으로 제시(수·유형 비교 없이) | High 반려 (profile FC-07 "유무 이분 금지", spec 8 — 정류소 수·마을버스·간선 수로만) |
| F18 | FC-08 배후 주거를 "전체 거주 인구"로 표현(K-apt는 아파트 의무관리 단지만) / geocode 미매칭 349개를 "아파트 없음"으로 단정 / geocode_신뢰도 무시하고 low·none 좌표를 확정 위치로 사용 / 아파트 footprint 근사치를 정밀 경계·동/호수로 표현 | High 반려 (profile FC-08 경계, spec §3·§8, data-integrity-check 이식 감사 — 89.7%만 좌표·소형 빌라 누락) |
| F19 | FC-01 총 유동인구 절대값·분위를 상권 간 순위·판정 positive 근거로 사용 ("유동 서울 상위 N%이므로 추천") — 밀도(유동/㎡) 대신 총량 분위를 씀 | High 반려 (길단위인구 = 폴리곤 내 도로구간 합, 면적 의존. 잠실역 총량분위 74.7 vs 밀도분위 7.8. FC-01 주력값은 유동/상권면적 분위, 총량은 규모 등급으로만. profile FC-01·spec §3) |
| F20 | 지점 후보에 붙인 상권/행정동 배경값(FC-01·11·30~32 등)을 지점 고유값처럼 표현 — `grain_is_proxy`·host 상권 관계·거리 없이 "이 자리 유동밀도는 서울 상위 X%" | High 반려 (spec §1-2, 지점≠상권 centroid. 포함/최근접 상권 또는 행정동 배경값임을 `grain_notes`·`proxy_note`·`host_commercial_area`로 명시. FC-07·08만 지점 반경 고유값) |
| F21 | 상권 폴리곤을 후보로 내보냄("잠실역 상권 추천") — 지점(아파트·역) seed가 있는데 폴리곤 단위로 후보 생성 | High 반려 (spec §1 재설계. 상권 폴리곤은 서울 27%·크기 20배 편차. seed 0개일 때만 상권/행정동 fallback, 그때 `candidate_type=commercial_area`·`precision=상권` 명시) |
| F22 | FC-06b 단기(방문형) 외국인을 상주인구로 나눔 / FC-06a·b 근사 비율(장기÷상주, 단기÷유동)을 "신호" 표기 없이 정밀 비율처럼 제시 / 방문 근사 비율을 유동/체류 방법론 차이 명시 없이 사용 | High 반려 (spec §8. FC-06a=장기÷상주, FC-06b=단기÷유동일평균. 모두 생활인구÷타 방법론 → "근사·신호". 정밀 비율은 내국인 생활인구) |
| F23 | `entry_health_v1` 등급(주의·경계)을 `fit_tier` 게이트·정렬키로 사용("진입환경 경계이므로 자동 조건부") / 등급을 대상 업종(커피)의 진입 건전성으로 해석 / 산식·컷·입력 분위 비공개로 등급만 노출 / 커피 폐업률·개업률로 등급 재계산 | High 반려 (entry-health-v1-cut-design.md. FC-10 = **전 업종 통합 지역 배경** 등급, 업종 구분 없음. 판정에선 반대근거 1항목. 산식 `0.25×4`·컷(상권 41/51/62·행정동 42/52/61)·입력 분위 4개 항상 공개. `score_is_predictive=false`) |
| F24 | 다른 지역 또는 `partial_*` Kakao manifest의 POI를 후보에 연결 / FD6·CE7 밀도를 FC·수요·공실·성공 outcome으로 표현하거나 `fit_tier`·정렬·긍정/반대 근거를 바꿈 | High 반려 (요청 시도·시군구·법정동과 정확히 일치하는 `complete_requested_queries` context만 허용. POI evidence는 `spatial_grain=지점`·snapshot 출처·한계를 기록하는 중립 관측이며 FC가 아니다. context 유무 비교에서 후보 ID/순서·등급·긍정/반대 근거 불변을 검사) |
| F25 | POI 카테고리 표시명(예: `음식점`)이 변수 재사용으로 FC-11 `상권_변화_지표` 코드(LL/LH/HL/HH)를 덮어써 지역 특성·`entry_health` 근거를 잘못 표기 | High 반려 (POI 카테고리명은 별도 `category_label` 변수만 사용. context 있음/없음 결과에서 FC-11 evidence 값·`entry_health_v1.inputs.라벨`도 동일한 공식 코드인지 검사) |
| F26 | '신호 없음' FC(FC-01 유동밀도·FC-30 점포밀도·FC-02·05·06a·06b·40·41·51·53)를 `reasons`/`counter_evidence`에 넣어 `fit_tier` 판정·정렬에 반영 / "유동밀도 서울 상위 N%이므로 추천" / 근거 수(`-len(reasons)`)로 같은 등급 안 순위를 매김 / 약한 배경 신호(FC-08·31·07 bus_n) 1개만으로 추천 승격 / FC-32 프랜차이즈 비율을 신규 진입 유불리 근거로 사용 | High 반려 (`feature-evidential-value.md` §9·프로파일 §9-1. 신호 없음 FC는 `context_notes`에만 — 판정·정렬·`data_confidence` 미반영. 약한 배경 신호는 `reasons` 2개 이상 + blocking 없음일 때만 추천, 전부 약한 신호면 `data_confidence` 하향. 정렬은 tier→반대근거 수→신뢰도→`candidate_id`) |
| F28 | 격자 생성 좌표(`candidate_type=생성지점_격자` / `GEN-PT-…`)를 실제 매물·점포처럼 표현 / `precision`을 `매물주소`·`지점`으로 / `listing_url`·`address_point` 부여 / `synthetic_anchor` 누락 / `place_name`을 "OO상가"처럼 실재하는 장소로 / 생성 사실·연결 `evidence_id`를 카드에 숨김 / 격자 좌표의 반경 인허가·POI 수를 매출·공실·수요·성공으로 해석 | High 반려 (`generated-evidence-schema.json`·spec §1-0. 생성지점은 `synthetic_anchor=true`·`spatial_grain=precision=지점(생성)`·`listing_url=address_point=null` 강제, `context_notes[0]`에 생성 고지, `evidence_origin=project_generated`. 반경 카운트는 접근성·상업활성 맥락이지 성과 지표 아님) |
| F27 | 추정매출 `당월_매출_금액`을 월 매출로 표기(단위 `원/점포·월`) / 추정매출 극소값(< ₩10만/분기, 표본 1~2건)을 "매출 규모 작음" 반대 근거로 사용 / 상권 매출 미제공(커버리지 54%)을 상권배후지·행정동 매출로 fallback해 `sales_per_store`를 채우고 tier를 올림 / 미제공을 "이 조건 나쁨"으로 해석 | High 반려 (`value-level-qa.md`. `당월_매출_금액`은 분기 합계 — 단위 `원/점포·분기`. 극소값은 결측 취급(`< ₩30만/분기`). **매출 미제공은 fallback 금지**(표본 부족 = 소규모 시장 신호, 넓은 grain 대체 시 과대평가) — `sales_thin_market`으로 `조건부 검토` 유지 + "추정매출 미제공 — 카드거래 표본 임계치 미만(소규모 시장 가능성), 매출 검증 불가"로 기록) |
| F30 | 상가건물(`candidate_type=상가건물_인근`, `BLDG-{건물관리번호}`) 후보의 Tier2 도로명주소를 `address_point`에 넣거나 `precision`을 `매물주소`로 승격 / `synthetic_anchor` tier 캡(`추천`→`조건부 검토`)을 적용 / `context_notes[0]`의 "매물 아님" 강제 고지를 생략 / 건물 전체 주소를 특정 임대 호실 주소처럼 표현("OO상가 2층 매물") | High 반려 (candidate-selection-spec.md §1-0 상가건물 항목, rag-evidence-schema.json `building_address`. 건물 주소는 `building_address`에만, `address_point`는 여전히 매물 전용. footprint가 실재 공공데이터라 `synthetic_anchor=false`·tier 캡 미적용, 대신 `context_notes[0]` 강제 문구로 약하게 제약) |
| F29 | 뉴스 지역 기사량(`FC-51_뉴스_{자치구,행정동}기사량`·`FC-51_네이버뉴스_*`)을 `reasons`/`counter_evidence`/`fit_tier`/정렬에 반영 / "재개발 기사 N건이므로 성장 기대 → 추천" / 기사량을 사업 확정·추진단계·상권 수요·매출·공실로 해석 / `topic_match` 통과 수를 "시설·개발로 필터링된 기사"로 표현(실제로는 이미 주제 한정 snapshot) / 성격 다른 빅카인즈·네이버 row 수를 합산 / 빅카인즈 `키워드`·`특성추출`·`위치` 전체를 부분문자열 지역 매칭에 사용해 곁다리 지명(능동/길동/번동)을 그 행정동 기사량으로 계상 / 네이버 `description` 스니펫을 지역 매칭에 사용 / `is_stale=true`(관측 종료일 기준 45일 초과)인 오래된 snapshot을 최신 보도 상황으로 표현하거나 stale 플래그로 `fit_tier`를 낮춤 | High 반려 (`handoff_bigkinds_news.md`·`handoff_naver_news_snapshot.md`·프로파일 §9-1. FC-51-news는 "신호 없음(서술만)" — `evidence[]`·`context_notes`에만, 판정·정렬·`data_confidence` 미반영. news on/off 비교에서 후보 ID·순서·등급·긍정/반대 근거·위치·신뢰도 불변 검사. 지역 매칭 = 빅카인즈 제목+`위치` 토큰 정확일치, 네이버 제목만. `topic_match_rate`≈1.0이면 "주제 한정 snapshot 내 지역 매칭 수"로 표기(부분집합 아님). row 수는 `sources[]` source별로만. `is_stale`은 `source_freshness`·`context_notes` 재수집 신호이며 등급·정렬 불변) |
